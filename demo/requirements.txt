# Demo dependencies (install inside a virtual environment — preferably with conda)
# Note: synaptik_core (Rust/Python binding) is built locally; install it separately.

python-dotenv>=1.0.0
openai>=1.30.0
rich>=13.7.0
synaptik-core-beta==0.1.1
